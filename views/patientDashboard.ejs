<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Patient Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <%- include('./navbar') %>
    

  <div class="container mt-5">
    <h2 class="mb-4 text-center text-primary">👤 Welcome to the Your Dashboard <%= patient.name %></h2>

    <div class="card shadow-sm p-4">
      <h4 class="text-secondary mb-3">Patient Information</h4>

      <p><strong>Username:</strong> <%= patient.username %></p>
      <p><strong>Age:</strong> <%= patient.age %></p>
      <p><strong>Gender:</strong> <%= patient.gender %></p>
      <p><strong>Contact:</strong> <%= patient.contact %></p>
      <p><strong>Address:</strong> <%= patient.address %></p>
      <p><strong>Medical History:</strong> <%= patient.medicalHistory.join(", ") %></p>
      <p><strong>Registered On:</strong> <%= new Date(patient.createdAt).toLocaleString() %></p>
      <div class="d-flex flex-wrap justify-content-center gap-3">
        <a href="/dashboard/appointments" class="btn btn-info btn-lg shadow">📅 Appointments</a>
        <a href="/dashboard/admissions" class="btn btn-warning btn-lg shadow">🏥 Admission</a>
        <a href="/dashboard/discharge" class="btn btn-danger btn-lg shadow">🚪 Discharge</a>
        <a href="/dashboard/communications" class="btn btn-secondary btn-lg shadow">✉️ Communication</a>
      </div>
    </div>
  </div>

  <%- include('./footer') %></body>
</html>
