<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Staff Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .card-title i {
      margin-right: 8px;
    }
  </style>
</head>
<body>
    <%- include('./navbar') %>
    <div class="container mt-5">
        <h2 class="text-center mb-4">👥 Staff Dashboard</h2>
      
        <div class="card p-4 shadow-sm">
          <h4>Welcome, <%= staff.name %> 👋</h4>
          <p><strong>Username:</strong> <%= staff.username %></p>
          <p><strong>Role/Department:</strong> <%= staff.department || 'N/A' %></p>
          <p><strong>Contact:</strong> <%= staff.contact || 'N/A' %></p>
          <p><strong>Joined On:</strong> <%= staff.createdAt.toDateString() %></p>
          <div class="row row-cols-1 row-cols-md-2 g-4">
            <div class="col">
              <a href="/dashboard/staff/roster" class="btn btn-outline-primary w-100 p-4 shadow">📋 Staff Roster</a>
            </div>
            <div class="col">
              <a href="/dashboard/staff/attendance" class="btn btn-outline-success w-100 p-4 shadow">🕒 Attendance Tracking</a>
            </div>
            <div class="col">
              <a href="/dashboard/staff/department" class="btn btn-outline-warning w-100 p-4 shadow">🏢 Department Overview</a>
            </div>
            <div class="col">
              <a href="/dashboard/staff/notifications" class="btn btn-outline-danger w-100 p-4 shadow">🔔 Notifications</a>
            </div>
          </div>
        </div>
      </div>


  <!-- Bootstrap Icons -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.js"></script>
  <%- include('./footer') %>
</body>
</html>
